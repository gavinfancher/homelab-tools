[user]
	email = gavin@email.com
	name = gavin




[alias]
	flow = "!f() { \
		read -rp 'Enter commit message: ' msg; \
		if [ -z \"$msg\" ]; then \
			echo 'Commit message cannot be empty.'; \
			return 1; \
		fi; \
		git add .; \
		git commit -m \"$msg\"; \
		git push; \
	}; f"